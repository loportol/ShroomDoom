using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlateEvent
{
    public int num;
    public bool reset;

    public PlateEvent(int _new_num, bool _new_reset) {
        num = _new_num;
        reset = _new_reset;
    }

}
